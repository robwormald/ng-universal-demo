// import { NgModuleFactoryLoader, NgModuleFactory } from '@angular/core';
// import * as path from 'path';

// declare var require;
// export class ServerRouterLoader implements NgModuleFactoryLoader {
// //   load(rawPath):Promise<NgModuleFactory<any>> {
// // 	  const [pathToModule, moduleName] = rawPath.split('#');
// // 	  const resolvedPath = './' + path.join('.','ngfactory/src', pathToModule) + '.ngfactory'
// // 	  console.log('resolving...', resolvedPath)
// // 	  return new Promise((resolve, reject) => {




// //           console.log(resolvedPath)
// // 		  const mod = require('./lazy' );

// // 		  return mod[moduleName + 'NgFactory'];
// // 	  });
// //   }
// }
